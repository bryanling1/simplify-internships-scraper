export const Regex = {
    page: {
        simplify: {
            compensation: {
                hourlyRange: /\$([0-9\.]+) - \$([0-9\.]+)Hourly/,
                hourlyCurrency: /([A-Z]*)\$([0-9\.]+)Hourly/
            }
        }
    }
}